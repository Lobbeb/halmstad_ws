# pip-installable runtime dependencies (rclpy provided by ROS; do NOT pip-install it)
PyYAML>=6.0
pandas>=1.5
numpy>=1.24,<2
matplotlib>=3.7
rospkg>=1.4.1
onnx>=1.16
onnxruntime>=1.18; platform_system != "Windows"
onnxruntime-directml>=1.18; platform_system == "Windows"

# developer/test tooling (install only on dev machines)
pytest>=7.0
black
flake8
mypy
pre-commit
